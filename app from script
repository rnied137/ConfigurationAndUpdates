unset QT_PLUGIN_PATH   
appname=$(basename "$0" .sh)
dirname=$(dirname "$0")
cd "$dirname/bin"
export LD_LIBRARY_PATH=`pwd`
./$appname "$@"



Main folder
./ClientQt.sh
bin/ClientQT
bin/*.so
platforms/ *.so (from platforms folder)

